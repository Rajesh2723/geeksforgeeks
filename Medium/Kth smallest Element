int kthSmallest(int arr[], int l, int r, int k) {
        vector<int>v;
        for(int i=l;i<=r;i++){
            v.push_back(arr[i]);
        }
        sort(v.begin(),v.end());
        return v[k-1];
    }
